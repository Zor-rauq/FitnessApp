# Legacy workflow disabled: GitHub Pages is configured via the gh-pages branch.
# This file is kept as a no-op to avoid conflicts with the branch-based deployment.
name: Legacy pages deploy (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Use the workflow in github-pages.yml instead."
